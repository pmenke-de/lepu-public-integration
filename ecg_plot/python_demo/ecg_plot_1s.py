

import matplotlib.pyplot as plt

# this demo show how to plot ECG
def draw_ecg(data, sampling_rate=125):



# plot background grid
def draw_bkg(figure):
    # we are use speed at 25 mm/sec, so for 1 second data, we need plot 5 large boxes and 25 small boxes in x-axis.

if __name__ == '__main__':

    # plot background grid
    fig = plt.figure(50, 20)
